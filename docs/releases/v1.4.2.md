# DeskBox 1.4.2

DeskBox 1.4.2 focuses on correctness and tactile polish across Todo, Quick Capture, music, and widget groups. Task completion now remains attached to the intended item, image clipboard records behave as images from capture through copy, attachments share one visual component, and rapid circular group scrolling no longer stalls behind time-based input blockers.

## Highlights

- **Todo checks stay on the intended task.** Completion updates are item-based, including custom sorting, filters, recycled rows, and removal from the Completed view.
- **Quick Capture treats clipboard images as images.** Both list and detail panes show thumbnails, and copying an image record writes bitmap content instead of a local path.
- **Attachments share one component.** Todo and Quick Capture use equal square tiles with image thumbnails or file icons, horizontal scrolling, and hover removal.
- **Native-feeling music transport.** Filled play, pause, previous, and next icons share one responsive size; cover mode receives a taller frosted control strip and clearer single-line information layout.
- **Continuous circular group navigation.** Rapid wheel and precision-touchpad input is no longer blocked by stacked cooldowns. Effective-step accumulation, latest-target cancellation, and first/last looping remain.
- **A shorter, action-first onboarding flow.** New users create a file widget, learn the tray, and try global show/hide before choosing optional feature widgets. Those switches apply immediately and stay synchronized with Settings.
- **Empty, Markdown, search, and layer states are safer.** Records and Pinned retain their empty views, Pinned creates pinned content directly, Markdown images and rich blocks lay out more reliably, and search/layer maintenance avoids stale or unbounded work.
- **Safer note navigation and clearer first use.** Moving away from a new note saves meaningful content first and never lets an empty draft block selection. Newly created Todo and Quick Capture widgets can start with editable, localized example content that users may keep, change, or remove.
- **More stable reading and navigation surfaces.** Markdown resolves readable light/dark foregrounds and accessible semantic accents; background search refreshes retain stable result rows and resolved icons; collapsed groups show their current position without changing the expanded title layout.
- **Small visual details are aligned.** The floating title drag grip keeps its collapse animation without a center seam, and weekly high/low temperatures now share the same type scale as their day labels.

## Release artifacts

The GitHub Release includes both framework-dependent Direct installers and their SHA-256 sidecars. The Microsoft Store submission package is produced separately for Partner Center and is not attached to GitHub.

- `DeskBox_Setup_1.4.2_x64.exe` is the framework-dependent Direct installer for most Intel and AMD PCs.
- `DeskBox_Setup_1.4.2_arm64.exe` is the framework-dependent Direct installer for Windows on ARM PCs.
- `DeskBox_1.4.2.0_x64_arm64.msixupload` is the combined Microsoft Store submission package.
- Final sizes and SHA-256 checksums from the release build are recorded below.

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| `DeskBox_Setup_1.4.2_x64.exe` | 24.19 MiB | `818621BFAE640CE8A9375CD7B170D977FFAE0487EEB86C359536FE42E6CDDE7E` |
| `DeskBox_Setup_1.4.2_arm64.exe` | 24.15 MiB | `9DD07070F190E0C079B5F3A2539A89BB88DA38277E8361E3BA2F59E000D296DC` |
| `DeskBox_1.4.2.0_x64_arm64.msixupload` | 257.65 MiB | `72BF122D01ED63E8C65FA76CC396FCC6D2AAFC434FFE97D4CD6036CED224C0CD` |

The Direct installers are not Authenticode-signed. The Store submission package uses the Partner Center identity and is uploaded unsigned for Store signing.

## 中文说明

DeskBox 1.4.2 集中修复待办、随记、音乐和格子组中会直接影响操作感受的问题。待办勾选现在始终对应实际任务；剪贴板图片从采集、缩略图展示到再次复制都保持图片语义；附件统一使用同一组件；格子组快速循环滚动也不会再被时间限制卡住。

## 更新亮点

- **待办勾选对应正确任务。** 自定义排序、筛选、列表行复用以及从“已完成”移出时，都按任务本身更新。
- **随记剪贴板图片保持图片语义。** 左侧列表和右侧详情都显示缩略图，再次复制时写入图片，而不是本地路径。
- **附件统一为同一组件。** 待办与随记使用等尺寸方块，图片展示缩略图，文件展示图标，支持横向滚动和悬浮删除。
- **音乐控制更接近 Windows 原生。** 播放、暂停、上一首和下一首使用同尺寸面性图标；封面模式增加更高的磨砂控制条，并整理为清晰的单行信息布局。
- **格子组可以连续循环切换。** 快速滚轮和触控板输入不再被双重冷却拦截，同时保留有效步长合并、最终目标优先与首尾循环。
- **新手引导缩短并优先让用户实际操作。** 用户会先新建文件格子、认识托盘并体验全局显示与隐藏，最后再选择需要的功能格子；开关立即生效，并与设置保持同步。
- **空状态、Markdown、搜索和层级更可靠。** 随记与固定标签保持正确空状态，固定页可直接创建固定内容，Markdown 图片与复杂块布局更稳定，搜索和桌面层维护不会留下无界或过期工作。
- **随记切换更可靠，首次使用更清晰。** 从新建随记切换到其他内容时，会先保存有效内容；空白草稿不会阻止切换。首次创建待办和随记格子时，可显示可编辑、可删除的本地化示例内容。
- **阅读与导航界面更稳定。** Markdown 在浅色和深色主题下会使用可读前景色和足够对比度的语义色；搜索后台刷新会保留稳定的结果行和已解析图标；收起的格子组可展示当前位置，不影响展开状态的标题布局。
- **细节视觉进一步统一。** 悬浮标题拖动横条保留收起动画并消除中间接缝；天气周视图的高低温字号与日期标签统一。

## 发布产物

GitHub Release 提供两个架构的框架依赖直装包及 SHA-256 校验文件。Microsoft Store 提交包单独用于 Partner Center，不附加到 GitHub。

- `DeskBox_Setup_1.4.2_x64.exe` 是适用于大多数 Intel 和 AMD 电脑的框架依赖直装包。
- `DeskBox_Setup_1.4.2_arm64.exe` 是适用于 Windows on ARM 电脑的框架依赖直装包。
- `DeskBox_1.4.2.0_x64_arm64.msixupload` 是合并 x64 与 ARM64 的 Microsoft Store 提交包。
- 下表记录发布构建的最终大小与 SHA-256 校验值。

| 产物 | 大小 | SHA-256 |
| --- | ---: | --- |
| `DeskBox_Setup_1.4.2_x64.exe` | 24.19 MiB | `818621BFAE640CE8A9375CD7B170D977FFAE0487EEB86C359536FE42E6CDDE7E` |
| `DeskBox_Setup_1.4.2_arm64.exe` | 24.15 MiB | `9DD07070F190E0C079B5F3A2539A89BB88DA38277E8361E3BA2F59E000D296DC` |
| `DeskBox_1.4.2.0_x64_arm64.msixupload` | 257.65 MiB | `72BF122D01ED63E8C65FA76CC396FCC6D2AAFC434FFE97D4CD6036CED224C0CD` |

直装包未进行 Authenticode 签名。商店提交包使用 Partner Center 正式身份，由 Microsoft Store 在提交后签名。

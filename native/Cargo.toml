[workspace]
members = [
    "deskbox-native",
    "deskbox-audio-session-fixture",
    "deskbox-search-core",
]
resolver = "3"

[profile.dev]
panic = "abort"

[profile.release]
codegen-units = 1
lto = "thin"
panic = "abort"
strip = "none"
